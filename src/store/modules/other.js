
const user = {
  state: {
    centerUserBase: { }
  },

  mutations: {
    SET_CENTER_USER_BASE: (state, centerUserBase) => {
      state.centerUserBase = centerUserBase
    }
  }

}

export default user
