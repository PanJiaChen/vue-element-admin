const {{name}} = {
  {{#if state}}
  state: {
    {{name}}: {}
  },
  {{/if}}
  {{#if mutations}}
  mutations: {
    {{#if state}}
    resetState{{name}}(state) {
      state = {}
    }
    {{/if}}
  },
  {{/if}}
  {{#if actions}}
  actions: {},
  {{/if}}
  {{#if getters}}
  getters: {
    get{{name}}: (state) => {
      return state.{{name}}
    }
  }
  {{/if}}
}

export default {{name}}
